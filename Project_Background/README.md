# Project card backgrounds

Add one image per project and name it to match the `backgroundImage` field in that project’s JSON under `projects/` (for example `lobster.jpg` for `projects/lobster.json`).

Supported: common web formats (`.jpg`, `.jpeg`, `.png`, `.webp`, etc.).

If a file is missing, the card still works and shows the default solid background.
